name: release
on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install deps
        run: echo "installing deps"
      - name: Lint
        run: echo "linting ok"
      - name: Run tests
        run: |
          echo "running tests"
          exit 1
      - name: Publish
        run: echo "publishing"
